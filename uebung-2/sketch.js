let c = 30;

function setup() {
  createCanvas(800, 800);
  strokeCap(PROJECT);
}

function draw() {
  background(0);
  stroke(230, 150, 30);
  fill(255, 60);
  let a = 6;
  let side = 100;

  let abstand = 25;
  stroke(255)
  strokeWeight(1)

  fill(0, 0);
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);

  a += abstand;
  ellipse(a, height / 2, a, a);


  rect(mouseX, mouseY, 40, 40, 40);


}
